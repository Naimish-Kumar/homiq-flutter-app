import 'package:homiq/ui/screens/chat/model/chat_message_model.dart';

ChatMessage filterMessageType(ChatMessage message) {
  // This is a placeholder for the actual logic that would return
  // specific subtypes of ChatMessage if they existed.
  return message;
}
